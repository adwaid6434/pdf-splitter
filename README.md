This site processes PDFs entirely inside the user's browser.

The PDF never leaves the user's device.

All PDF operations happen locally.

No upload to server.

No API call.

No storage.

No database.

Everything is client-side nothing is logged.
